{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}E{fmt(s.total || 0)}f{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}z{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}U{fmt(s.total || 0)}p{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}T{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}W{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}U{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}n{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}3{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}C{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}G{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}R{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}2{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}L{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}L{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}i{fmt(s.total || 0)}1{fmt(s.total || 0)}8{fmt(s.total || 0)}n{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}/{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}/{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}I{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}V{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}X{fmt(s.total || 0)}A{fmt(s.total || 0)}x{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}Y{fmt(s.total || 0)}A{fmt(s.total || 0)}x{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}p{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}A{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}A{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}O{fmt(s.total || 0)}n{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}O{fmt(s.total || 0)}n{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}R{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}R{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}A{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}y{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}y{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}/{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}/{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}L{fmt(s.total || 0)}a{fmt(s.total || 0)}z{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}/{fmt(s.total || 0)}A{fmt(s.total || 0)}d{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}/{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}d{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}
{fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}z{fmt(s.total || 0)}y{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}T{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}z{fmt(s.total || 0)}y{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}z{fmt(s.total || 0)}y{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}z{fmt(s.total || 0)}y{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}@{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}K{fmt(s.total || 0)}P{fmt(s.total || 0)}I{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}T{fmt(s.total || 0)}A{fmt(s.total || 0)}T{fmt(s.total || 0)}U{fmt(s.total || 0)}S{fmt(s.total || 0)}_{fmt(s.total || 0)}C{fmt(s.total || 0)}O{fmt(s.total || 0)}L{fmt(s.total || 0)}O{fmt(s.total || 0)}R{fmt(s.total || 0)}S{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}2{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)}5{fmt(s.total || 0)}%{fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}4{fmt(s.total || 0)}5{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}9{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}_{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}2{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}7{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}1{fmt(s.total || 0)}4{fmt(s.total || 0)}5{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}6{fmt(s.total || 0)}5{fmt(s.total || 0)}%{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}4{fmt(s.total || 0)}5{fmt(s.total || 0)}%{fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}2{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}5{fmt(s.total || 0)}%{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}7{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)}5{fmt(s.total || 0)}%{fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}O{fmt(s.total || 0)}N{fmt(s.total || 0)}T{fmt(s.total || 0)}H{fmt(s.total || 0)}_{fmt(s.total || 0)}N{fmt(s.total || 0)}A{fmt(s.total || 0)}M{fmt(s.total || 0)}E{fmt(s.total || 0)}S{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}F{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}A{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}A{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}O{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}N{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}z{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}B{fmt(s.total || 0)}R{fmt(s.total || 0)}'{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}F{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}A{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}A{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}O{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}N{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}z{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}F{fmt(s.total || 0)}e{fmt(s.total || 0)}b{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}A{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}A{fmt(s.total || 0)}u{fmt(s.total || 0)}g{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}O{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}N{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}F{fmt(s.total || 0)}e{fmt(s.total || 0)}b{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}A{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}J{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}A{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}O{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}N{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}'{fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}R{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}T{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}>{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}>{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}>{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}>{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}L{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}A{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}s{fmt(s.total || 0)}L{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}O{fmt(s.total || 0)}n{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}Ã{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}E{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}Ã{fmt(s.total || 0)}©{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}!{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}O{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}`{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}`{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}/{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}Ã{fmt(s.total || 0)}©{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}Ã{fmt(s.total || 0)}©{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}­{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}I{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}x{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}Ã{fmt(s.total || 0)}­{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}E{fmt(s.total || 0)}f{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}Ã{fmt(s.total || 0)}­{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}Ã{fmt(s.total || 0)}µ{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}Ã{fmt(s.total || 0)}¡{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}!{fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}!{fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}A{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}y{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}K{fmt(s.total || 0)}P{fmt(s.total || 0)}I{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}E{fmt(s.total || 0)}U{fmt(s.total || 0)}R{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}E{fmt(s.total || 0)}U{fmt(s.total || 0)}R{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}U{fmt(s.total || 0)}r{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}U{fmt(s.total || 0)}r{fmt(s.total || 0)}l{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}A{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}<{fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}y{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}!{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}!{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}!{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}q{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}?{fmt(s.total || 0)}.{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}A{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}y{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}_{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}q{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}!{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}E{fmt(s.total || 0)}U{fmt(s.total || 0)}R{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}!{fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}U{fmt(s.total || 0)}r{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}_{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}†{fmt(s.total || 0)}’{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}R{fmt(s.total || 0)}L{fmt(s.total || 0)}S{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}Ã{fmt(s.total || 0)}¡{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}`{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}z{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}†{fmt(s.total || 0)}’{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Y{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}I{fmt(s.total || 0)}S{fmt(s.total || 0)}O{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}x{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}A{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Y{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}I{fmt(s.total || 0)}S{fmt(s.total || 0)}O{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}U{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}q{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}5{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}x{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}A{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}_{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}q{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}I{fmt(s.total || 0)}S{fmt(s.total || 0)}O{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}x{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}A{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}q{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}K{fmt(s.total || 0)}P{fmt(s.total || 0)}I{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}_{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}Ã{fmt(s.total || 0)}³{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)}s{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}U{fmt(s.total || 0)}s{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}x{fmt(s.total || 0)}({fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}3{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}Ã{fmt(s.total || 0)}³{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}Ã{fmt(s.total || 0)}¡{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}K{fmt(s.total || 0)}P{fmt(s.total || 0)}I{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Y{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Y{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}b{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}_{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}_{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}x{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}x{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}I{fmt(s.total || 0)}S{fmt(s.total || 0)}O{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}_{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}I{fmt(s.total || 0)}S{fmt(s.total || 0)}O{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}b{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}8{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}O{fmt(s.total || 0)}N{fmt(s.total || 0)}T{fmt(s.total || 0)}H{fmt(s.total || 0)}_{fmt(s.total || 0)}N{fmt(s.total || 0)}A{fmt(s.total || 0)}M{fmt(s.total || 0)}E{fmt(s.total || 0)}S{fmt(s.total || 0)}[{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}O{fmt(s.total || 0)}N{fmt(s.total || 0)}T{fmt(s.total || 0)}H{fmt(s.total || 0)}_{fmt(s.total || 0)}N{fmt(s.total || 0)}A{fmt(s.total || 0)}M{fmt(s.total || 0)}E{fmt(s.total || 0)}S{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}-{fmt(s.total || 0)}-{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Y{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Y{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}-{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}2{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Y{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}-{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}2{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}-{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}[{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}I{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}m{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}`{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}T{fmt(s.total || 0)}A{fmt(s.total || 0)}T{fmt(s.total || 0)}U{fmt(s.total || 0)}S{fmt(s.total || 0)}_{fmt(s.total || 0)}C{fmt(s.total || 0)}O{fmt(s.total || 0)}L{fmt(s.total || 0)}O{fmt(s.total || 0)}R{fmt(s.total || 0)}S{fmt(s.total || 0)}[{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}#{fmt(s.total || 0)}8{fmt(s.total || 0)}8{fmt(s.total || 0)}8{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}q{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}q{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}U{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}m{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)}?{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}m{fmt(s.total || 0)}.{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}_{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}_{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}_{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}q{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}_{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}_{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}F{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}Y{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}I{fmt(s.total || 0)}S{fmt(s.total || 0)}O{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}2{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}q{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}Ã{fmt(s.total || 0)}º{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}Ã{fmt(s.total || 0)}©{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}­{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}J{fmt(s.total || 0)}S{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}Ã{fmt(s.total || 0)}¡{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}b{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)}/{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}B{fmt(s.total || 0)}y{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}B{fmt(s.total || 0)}y{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}B{fmt(s.total || 0)}y{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}B{fmt(s.total || 0)}y{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}v{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}v{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}P{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}[{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}"{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}E{fmt(s.total || 0)}f{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}<{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}T{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}>{fmt(s.total || 0)}({fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}E{fmt(s.total || 0)}f{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}!{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}4{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}`{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}E{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}.{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}_{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}*{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}={fmt(s.total || 0)}e{fmt(s.total || 0)}q{fmt(s.total || 0)}.{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}]{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}_{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}_{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)};{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}!{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}!{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}!{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}!{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}+{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}}{fmt(s.total || 0)}%{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}Ã{fmt(s.total || 0)}ª{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}n{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}/{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}/{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}U{fmt(s.total || 0)}p{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}/{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}/{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}?{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}={fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}W{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}3{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}K{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}g{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}/{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}/{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}?{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}={fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}T{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}/{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}?{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}={fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}k{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}U{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}?{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}={fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}H{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}U{fmt(s.total || 0)}r{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}U{fmt(s.total || 0)}r{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}b{fmt(s.total || 0)}j{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}W{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}4{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}1{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}V{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}Â{fmt(s.total || 0)}·{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}Ã{fmt(s.total || 0)}­{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}G{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}O{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}V{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}V{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}I{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}I{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}I{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}I{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}/{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}g{fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}W{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}B{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}G{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}F{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}‘{fmt(s.total || 0)}‹{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}­{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}T{fmt(s.total || 0)}A{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}8{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}9{fmt(s.total || 0)}8{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}‘{fmt(s.total || 0)}¤{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}C{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}'{fmt(s.total || 0)}O{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}'{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}R{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}[{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}V{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}V{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}­{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}š{fmt(s.total || 0)}—{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}O{fmt(s.total || 0)}r{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}“{fmt(s.total || 0)}‹{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}D{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}µ{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}â{fmt(s.total || 0)}š{fmt(s.total || 0)}™{fmt(s.total || 0)}ï{fmt(s.total || 0)}¸{fmt(s.total || 0)}{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}H{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}H{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}Ã{fmt(s.total || 0)}¡{fmt(s.total || 0)}g{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}'{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}H{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}[{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}‘{fmt(s.total || 0)}¤{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}V{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}š{fmt(s.total || 0)}—{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}“{fmt(s.total || 0)}‹{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}"{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}”{fmt(s.total || 0)}§{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}i{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}<{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}S{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}Â{fmt(s.total || 0)}·{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}U{fmt(s.total || 0)}p{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}s{fmt(s.total || 0)}L{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}!{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}/{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}W{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}/{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}s{fmt(s.total || 0)}L{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}Ž{fmt(s.total || 0)}{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}Ã{fmt(s.total || 0)}¡{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}s{fmt(s.total || 0)}L{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}s{fmt(s.total || 0)}L{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}`{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}O{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}u{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}j{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}O{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}u{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}A{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}µ{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}Ã{fmt(s.total || 0)}³{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}T{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}Ã{fmt(s.total || 0)}³{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}Ã{fmt(s.total || 0)}©{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}Ã{fmt(s.total || 0)}©{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}.{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}"{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}`{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}`{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}W{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}b{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}C{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}p{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}E{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}"{fmt(s.total || 0)}V{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}U{fmt(s.total || 0)}s{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}F{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}/{fmt(s.total || 0)}4{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}/{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}}{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}w{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}S{fmt(s.total || 0)}a{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}Q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}7{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}b{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}7{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}7{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}N{fmt(s.total || 0)}u{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}†{fmt(s.total || 0)}’{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}({fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}t{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}G{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}ð{fmt(s.total || 0)}Ÿ{fmt(s.total || 0)}Ž{fmt(s.total || 0)}‰{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}{{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}}{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}{{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}}{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}}{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}B{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}K{fmt(s.total || 0)}P{fmt(s.total || 0)}I{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}6{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}_{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}/{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}7{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}4{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}4{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}u{fmt(s.total || 0)}b{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}0{fmt(s.total || 0)}4{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}`{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}'{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}n{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}U{fmt(s.total || 0)}p{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}W{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}+{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}}{fmt(s.total || 0)}%{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}3{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}[{fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)},{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}]{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}`{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}m{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}O{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}F{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}L{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}C{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}V{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}­{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}Î{fmt(s.total || 0)}”{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}b{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}/{fmt(s.total || 0)}3{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}2{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}2{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}`{fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}+{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}}{fmt(s.total || 0)}%{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}d{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}y{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}1{fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}l{fmt(s.total || 0)}y{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}G{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}2{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}X{fmt(s.total || 0)}A{fmt(s.total || 0)}x{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}K{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}x{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}Y{fmt(s.total || 0)}A{fmt(s.total || 0)}x{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}x{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}f{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}4{fmt(s.total || 0)}5{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}F{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}x{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}0{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}k{fmt(s.total || 0)}`{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}v{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}p{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}`{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}c{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)}}{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}'{fmt(s.total || 0)}]{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}8{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}-{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}-{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}K{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}-{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}[{fmt(s.total || 0)}6{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}6{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}K{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}-{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}4{fmt(s.total || 0)}){fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}[{fmt(s.total || 0)}6{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}6{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)}]{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}:{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}W{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}B{fmt(s.total || 0)}R{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}E{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}e{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}1{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}P{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}P{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}K{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}K{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}x{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)}%{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}4{fmt(s.total || 0)}0{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}6{fmt(s.total || 0)}5{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}A{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}2{fmt(s.total || 0)}}{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}C{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}P{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}8{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}-{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}l{fmt(s.total || 0)}({fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}-{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}1{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}P{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}h{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}D{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}+{fmt(s.total || 0)} {fmt(s.total || 0)}T{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}E{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}P{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)}:{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}U{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}o{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}b{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}R{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}%{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}6{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}F{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}T{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}l{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}P{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}p{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}}{fmt(s.total || 0)}x{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}Ã{fmt(s.total || 0)}³{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}i{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}9{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}†{fmt(s.total || 0)}’{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}/{fmt(s.total || 0)}5{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}/{fmt(s.total || 0)}8{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}A{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}`{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}T{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}C{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}s{fmt(s.total || 0)}[{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}`{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}Â{fmt(s.total || 0)}·{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}d{fmt(s.total || 0)}u{fmt(s.total || 0)}e{fmt(s.total || 0)}_{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}/{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}g{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}z{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}B{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}.{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}V{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}B{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}L{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}G{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}F{fmt(s.total || 0)}l{fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}M{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}A{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}y{fmt(s.total || 0)}C{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}I{fmt(s.total || 0)}d{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}?{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}/{fmt(s.total || 0)}*{fmt(s.total || 0)} {fmt(s.total || 0)}R{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}*{fmt(s.total || 0)}/{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}h{fmt(s.total || 0)}2{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}!{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}A{fmt(s.total || 0)}r{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}y{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}({fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}({fmt(s.total || 0)}_{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}S{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}i{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}h{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}l{fmt(s.total || 0)}g{fmt(s.total || 0)}"{fmt(s.total || 0)} {fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}t{fmt(s.total || 0)}h{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}p{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}4{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)}d{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}Ã{fmt(s.total || 0)}º{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}Ã{fmt(s.total || 0)}§{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}o{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}Ã{fmt(s.total || 0)}£{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}q{fmt(s.total || 0)}u{fmt(s.total || 0)}i{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}p{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}-{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}S{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}v{fmt(s.total || 0)}e{fmt(s.total || 0)}h{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}}{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}.{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}'{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}[{fmt(s.total || 0)}v{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}v{fmt(s.total || 0)}.{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}]{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}l{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}({fmt(s.total || 0)}B{fmt(s.total || 0)}o{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}j{fmt(s.total || 0)}o{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}({fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}w{fmt(s.total || 0)} {fmt(s.total || 0)}D{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}_{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}){fmt(s.total || 0)}.{fmt(s.total || 0)}t{fmt(s.total || 0)}o{fmt(s.total || 0)}L{fmt(s.total || 0)}o{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}T{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}S{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}({fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}u{fmt(s.total || 0)}a{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}p{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}P{fmt(s.total || 0)}T{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)} {fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}2{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)},{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}2{fmt(s.total || 0)}-{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}g{fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}'{fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}'{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}s{fmt(s.total || 0)}G{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}?{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)}w{fmt(s.total || 0)}n{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}S{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}f{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}({fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}>{fmt(s.total || 0)} {fmt(s.total || 0)}o{fmt(s.total || 0)}.{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)}={fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}_{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}){fmt(s.total || 0)}?{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)}){fmt(s.total || 0)} {fmt(s.total || 0)}:{fmt(s.total || 0)} {fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}l{fmt(s.total || 0)}l{fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}r{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}({fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}y{fmt(s.total || 0)}={fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}}{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}j{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}f{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}t{fmt(s.total || 0)}w{fmt(s.total || 0)}e{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}3{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}:{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}u{fmt(s.total || 0)}m{fmt(s.total || 0)}b{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}/{fmt(s.total || 0)}7{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}/{fmt(s.total || 0)}6{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}p{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}x{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}k{fmt(s.total || 0)}e{fmt(s.total || 0)}M{fmt(s.total || 0)}o{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}l{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}u{fmt(s.total || 0)}p{fmt(s.total || 0)}p{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}a{fmt(s.total || 0)}c{fmt(s.total || 0)}k{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}w{fmt(s.total || 0)}i{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}-{fmt(s.total || 0)}1{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}p{fmt(s.total || 0)}y{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)} {fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}g{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}1{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}d{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}-{fmt(s.total || 0)}p{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}a{fmt(s.total || 0)}r{fmt(s.total || 0)}y{fmt(s.total || 0)}/{fmt(s.total || 0)}2{fmt(s.total || 0)}0{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}o{fmt(s.total || 0)}p{fmt(s.total || 0)}B{fmt(s.total || 0)}a{fmt(s.total || 0)}d{fmt(s.total || 0)}g{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}f{fmt(s.total || 0)}l{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)} {fmt(s.total || 0)}i{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}s{fmt(s.total || 0)}-{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)} {fmt(s.total || 0)}g{fmt(s.total || 0)}a{fmt(s.total || 0)}p{fmt(s.total || 0)}-{fmt(s.total || 0)}2{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}[{fmt(s.total || 0)}1{fmt(s.total || 0)}1{fmt(s.total || 0)}p{fmt(s.total || 0)}x{fmt(s.total || 0)}]{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}m{fmt(s.total || 0)}u{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}d{fmt(s.total || 0)}-{fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}r{fmt(s.total || 0)}e{fmt(s.total || 0)}g{fmt(s.total || 0)}r{fmt(s.total || 0)}o{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}m{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)}.{fmt(s.total || 0)}5{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}t{fmt(s.total || 0)}r{fmt(s.total || 0)}u{fmt(s.total || 0)}n{fmt(s.total || 0)}c{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}({fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}i{fmt(s.total || 0)}e{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}y{fmt(s.total || 0)}){fmt(s.total || 0)}?{fmt(s.total || 0)}.{fmt(s.total || 0)}n{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)} {fmt(s.total || 0)}'{fmt(s.total || 0)}â{fmt(s.total || 0)}€{fmt(s.total || 0)}”{fmt(s.total || 0)}'{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}Â{fmt(s.total || 0)}·{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}({fmt(s.total || 0)}`{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}r{fmt(s.total || 0)}v{fmt(s.total || 0)}i{fmt(s.total || 0)}c{fmt(s.total || 0)}e{fmt(s.total || 0)}.{fmt(s.total || 0)}${fmt(s.total || 0)}{{fmt(s.total || 0)}s{fmt(s.total || 0)}.{fmt(s.total || 0)}s{fmt(s.total || 0)}t{fmt(s.total || 0)}a{fmt(s.total || 0)}t{fmt(s.total || 0)}u{fmt(s.total || 0)}s{fmt(s.total || 0)}}{fmt(s.total || 0)}`{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)} {fmt(s.total || 0)}&{fmt(s.total || 0)}&{fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}Â{fmt(s.total || 0)}·{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}{{fmt(s.total || 0)}t{fmt(s.total || 0)}i{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}}{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)} {fmt(s.total || 0)}c{fmt(s.total || 0)}l{fmt(s.total || 0)}a{fmt(s.total || 0)}s{fmt(s.total || 0)}s{fmt(s.total || 0)}N{fmt(s.total || 0)}a{fmt(s.total || 0)}m{fmt(s.total || 0)}e{fmt(s.total || 0)}={fmt(s.total || 0)}"{fmt(s.total || 0)}m{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}o{fmt(s.total || 0)} {fmt(s.total || 0)}f{fmt(s.total || 0)}o{fmt(s.total || 0)}n{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}e{fmt(s.total || 0)}m{fmt(s.total || 0)}i{fmt(s.total || 0)}b{fmt(s.total || 0)}o{fmt(s.total || 0)}l{fmt(s.total || 0)}d{fmt(s.total || 0)} {fmt(s.total || 0)}s{fmt(s.total || 0)}h{fmt(s.total || 0)}r{fmt(s.total || 0)}i{fmt(s.total || 0)}n{fmt(s.total || 0)}k{fmt(s.total || 0)}-{fmt(s.total || 0)}0{fmt(s.total || 0)} {fmt(s.total || 0)}t{fmt(s.total || 0)}e{fmt(s.total || 0)}x{fmt(s.total || 0)}t{fmt(s.total || 0)}-{fmt(s.total || 0)}s{fmt(s.total || 0)}m{fmt(s.total || 0)}"{fmt(s.total || 0)}>{fmt(s.total || 0)}|{fmt(s.total || 0)}|{fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}s{fmt(s.total || 0)}p{fmt(s.total || 0)}a{fmt(s.total || 0)}n{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}}{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}>{fmt(s.total || 0)}){fmt(s.total || 0)}}{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}<{fmt(s.total || 0)}/{fmt(s.total || 0)}d{fmt(s.total || 0)}i{fmt(s.total || 0)}v{fmt(s.total || 0)}>{fmt(s.total || 0)}
{fmt(s.total || 0)} {fmt(s.total || 0)} {fmt(s.total || 0)}){fmt(s.total || 0)};{fmt(s.total || 0)}
{fmt(s.total || 0)}}{fmt(s.total || 0)}
